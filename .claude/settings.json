{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "//": "Deliberately minimal. settingSources:['project'] loads this file into every agent run, so anything added here becomes part of the lead agent's configuration."
}
